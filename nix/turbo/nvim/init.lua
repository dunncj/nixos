-- Entry point. home-manager writes this file to ~/.config/nvim/init.lua and
-- symlinks the lua/ tree next to it, so everything below is a real .lua file
-- you can edit with LSP and treesitter support like any other project.
require("turbo")
